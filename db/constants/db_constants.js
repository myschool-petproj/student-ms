const COMMON = {
    id: 'id',
    is_deleted: 'is_deleted'
};

const TABLES = {
    students: 'students'
};

const STUDENTS = {
    first_name: 'first_name',
    last_name: 'last_name',
};

module.exports = {
    COMMON,
    TABLES,
    STUDENTS
}